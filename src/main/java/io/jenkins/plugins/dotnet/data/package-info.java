/**
 * This package contains classes for processing related data (runtime identifiers, SDK downloads, &hellip;).
 * <p>
 * Currently this is based on embedded resources, but it could/should be switched to remote resources so changes do not require a
 * plugin release.
 */
package io.jenkins.plugins.dotnet.data;
